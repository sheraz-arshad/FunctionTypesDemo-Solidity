# Demo of function types in Solidity
The smart contract shows demo of how a function type can be dynamically assigned to a varible within a struct.

### To run the tests use the following command
```
 npm run test
```